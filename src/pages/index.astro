---
import Base from '../layouts/Base.astro';
import site from '../data/site.json';
---
<Base title={`${site.name} — ${site.tagline.en}`} description={site.description.en} locale="en" path="/">
  <main class="min-h-screen grid place-items-center">
    <h1 class="font-display text-6xl tracking-luxe uppercase">Bona</h1>
  </main>
</Base>
